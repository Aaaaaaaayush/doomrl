doom_scenario_path = basic.wad
doom_skill = 5

# Rewards
each_step_penalty = 1

# Rendering options
screen_resolution = RES_320X240
screen_format = GRAY8
render_hud = false

# Make episodes start in the first tic
episode_start_time = 1

# Make episodes finish after 300 tics
episode_timeout = 300

# Available buttons
available_buttons =
    {
        MOVE_LEFT
        MOVE_RIGHT
        ATTACK
    }

# Game variables to expose: index 0: HEALTH, index 1: AMMO2
available_game_variables = { HEALTH AMMO2 }
mode = PLAYER

doom_scenario_path = deadly_corridor.wad
doom_skill = 5

# Rewards
death_penalty = 100

# Rendering options
screen_resolution = RES_320X240
screen_format = GRAY8
render_hud = false

# Make episodes start in the first tic
episode_start_time = 1

# Make episodes finish after 2100 tics
episode_timeout = 2100

# Available buttons
available_buttons =
    {
        MOVE_LEFT
        MOVE_RIGHT
        ATTACK
        MOVE_FORWARD
        MOVE_BACKWARD
        TURN_LEFT
        TURN_RIGHT
    }

# Game variables to expose:
# index 0: HEALTH, index 1: AMMO2, index 2: POSITION_X, index 3: POSITION_Y
available_game_variables =
    {
        HEALTH
        AMMO2
        POSITION_X
        POSITION_Y
    }

mode = PLAYER
